apiVersion: v1
kind: Secret
metadata:
  name: ${repo_name}
  namespace: ${namespace}
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: ${repo_url}
  password: ${repo_password}
  username: ${repo_username}